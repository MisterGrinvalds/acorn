name: kubernetes
description: Kubernetes and Helm development tools
version: 1.0.0
category: cloud

requires:
  tools: []
  components: [shell]

provides:
  aliases: [k, kns, kctx]
  functions: [kuse, knsuse, kpods, klf]
  completions: [kubectl, helm]

xdg:
  config: ""
  data: ""
  cache: ""
  state: ""

platforms: [darwin, linux]
shells: [bash, zsh]

setup:
  brew: [kubectl, helm, k9s]
  apt: [kubectl]
  post_install: ""
