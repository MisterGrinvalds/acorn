name: tools
version: 1.0.0
description: System tools management and version checking utilities
category: core

requires:
  tools: []  # No hard requirements - gracefully handles missing tools
  components: [shell]

provides:
  aliases: [tools, tools-list, tools-check, tools-update, tools-missing, tools-install, versions, system-update, whichx]
  functions: [tools_status, list_tools, check_tools, update_tools, missing_tools, install_tool, quick_versions, smart_update, which_enhanced]
  completions: []

xdg:
  config: ""
  data: ""
  cache: ""
  state: ""

platforms: [darwin, linux]
shells: [bash, zsh]

setup:
  brew: []
  apt: []
  post_install: ""
