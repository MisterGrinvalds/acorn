name: database
description: Database tools and service management
version: 1.0.0
category: database

requires:
  tools: []
  components: [shell]

provides:
  aliases: [pg, my, mongo, redis, lite]
  functions: []
  completions: []

xdg:
  config: ""
  data: ""
  cache: ""
  state: ""

platforms: [darwin, linux]
shells: [bash, zsh]

setup:
  brew: [pgcli, mycli, mongosh, redis]
  apt: [postgresql-client, mysql-client, redis-tools, sqlite3]
  post_install: ""
